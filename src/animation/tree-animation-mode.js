export const TREE_ANIMATION_MODES = Object.freeze({
  HIERARCHICAL: 'hierarchical',
  REDUCED: 'reduced',
  PRIMARY_SWAY: 'primary-sway',
  CHEAP_SWAY: 'cheap-sway',
  STATIC: 'static',
});

export const TREE_ANIMATION_MODE_IDS = Object.freeze(
  Object.values(TREE_ANIMATION_MODES),
);
